<section id="container">
  <section>
    <p style="font-size: 22px;">
      <%= t('decks.new_deck') %>
    </p>
    <%= render 'form' %>
    <p><%= link_to t('forms.links.back'), decks_path %></p>
  </section>
</section>
