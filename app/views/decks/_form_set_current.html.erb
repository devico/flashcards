<div>
  <%= simple_form_for current_user, url: users_update_current_deck_path do |f| %>
    <%= f.input :current_deck, label: "Установить текущей колоду:", collection: current_user.decks.map { |d| [d.name, d.id] },:include_blank => false %>
    <%= f.submit 'Выбрать'%>
  <% end %>
</div>
