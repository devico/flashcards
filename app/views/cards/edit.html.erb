<section id="container">
  <section>
    <p style="font-size: 22px;">
      <%= t('cards.edit_card')%>
    </p>
    <%= render 'form' %>
    <%= link_to t('forms.links.show'), @card %>
    <%= link_to t('forms.links.back'), cards_path %>
  </section>
</section>
