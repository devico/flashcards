<section id="container">
  <section>
    <p style="font-size: 22px;">
      <%= t('cards.edit')%>
    </p>
    <%= render 'form' %>
    <%= link_to t('simple_form.links.show'), @card %>
    <%= link_to t('simple_form.links.back'), cards_path %>
  </section>
</section>
