<%= simple_form_for 'check', url: '/cards/check_card' do |f| %>
  <%= f.label @card.translated_text %>
  <hr>
  <%= f.input :card_id, as: :hidden, value: @card.id %>
  <%= f.input :user_text, label: 'Enter original text' %>
  <%= f.submit %>
<% end %>
