<section id="container">
  <section>
    <p style="font-size: 22px;">
      <%= t('cards.new') %>
    </p>
    <%= render 'form' %>
    <p><%= link_to t('links.back'), cards_path %></p>
  </section>
</section>
